[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install --omit=dev --ignore-scripts"]

[phases.build]
cmds = ["echo 'Skipping frontend build'"]

[start]
cmd = "node worker/worker.js"
